{
  "name": "pharmsita-api",
  "private": true,
  "version": "0.1.0",
  "description": "PharmSITA backend API scaffold",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
